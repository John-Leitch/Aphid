#'std';
#'process';
var count = 3;
exec = macro(@(n) { 0..count->@() #> ('aphid {0}.alx' :: [ quote(n) ]) });
exec(ArithmeticBenchmark);
exec(CallBenchmark);
exec(CustomOperatorBenchmark);
exec(IncludeBenchmark);