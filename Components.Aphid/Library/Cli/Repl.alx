var repl;

@{
    repl = {
        run: @{
            using Components.Aphid.UI;
            var r = new AphidRepl(new AphidInterpreter(this.'$parent'.'$scope'));

            try r.Run();
            finally r.Dispose();
        }
    }
}()