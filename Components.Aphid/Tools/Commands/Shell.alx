#'Std';

console.stdIn.lines(@{
    var si = new ProcessStartInfo('cmd', '/S /C "{0}"' :: ($_ ~: ['["^]', '^$0']));
    si.UseShellExecute = false;
    si.RedirectStandardInput = true;
    var p = Process.Start(si);
    p.WaitForExit();
});